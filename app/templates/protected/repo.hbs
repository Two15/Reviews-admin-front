{{organization-list selectedName=model didSelect=(route-action 'didSelectOrganization')}}
{{repository-list organization=model willEdit=(action (mut currentRepo))}}
{{#if currentRepo}}
  {{rmc-toggler repository=currentRepo}}
{{/if}}
